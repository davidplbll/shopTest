import { CustomDisableDirective } from './custom-disable.directive';

describe('CustomDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDisableDirective();
    expect(directive).toBeTruthy();
  });
});
